{
  "name": "question-paper-generator",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd question-paper-frontend && npm start",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
